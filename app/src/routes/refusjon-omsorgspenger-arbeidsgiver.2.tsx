import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/refusjon-omsorgspenger-arbeidsgiver/2")({
  component: () => <div>Hello /refusjon-omsorgspenger-arbeidsgiver/2!</div>,
});
